## 1. 基线与模块化

- [x] 1.1 记录当前模型查询、普通生成和流式生成的冷启动、热请求、首字时间、总时长与进程数量基线
- [x] 1.2 将配置、请求模型、协议适配和 Kiro 调用从 `main.py` 拆分为独立模块，并保持现有接口测试通过
- [x] 1.3 定义统一的 `KiroTransport`、内部生成事件、错误分类和取消接口
- [x] 1.4 将现有每请求 CLI 实现迁移为 `CliTransport`，作为后续传输的稳定回退
- [x] 1.5 增加结构化请求日志、request ID、传输类型、首字时间和总时长指标，并验证敏感字段脱敏

## 2. 模型发现缓存

- [x] 2.1 实现包含快照、TTL、最大陈旧窗口和来源信息的异步模型缓存
- [x] 2.2 实现 single-flight 刷新，保证并发过期请求只触发一次上游模型发现
- [x] 2.3 实现 stale-if-error 行为和超过陈旧窗口后的明确错误
- [x] 2.4 在认证变化、模型不存在和管理刷新操作时主动失效缓存并受控重试一次
- [x] 2.5 增加缓存命中、并发刷新、失败快照和主动失效测试
- [x] 2.6 运行基准并确认热请求不再产生每请求 `--list-models` 进程

## 3. 统一事件与真正流式输出

- [x] 3.1 为 CLI/ACP 上游实现增量 UTF-8 解码和内部文本、思考、工具、用量、结束、错误事件
- [x] 3.2 将 OpenAI Chat Completions SSE 改为实时增量 chunk、结束块和 `[DONE]`
- [x] 3.3 为 OpenAI Responses API 实现标准增量事件序列
- [x] 3.4 将 Anthropic Messages SSE 改为实时 message/content block/delta/stop 事件
- [x] 3.5 实现客户端断开检测、取消传播、宽限期和资源释放
- [x] 3.6 增加中文跨字节分块、流中错误、提前断开和慢客户端测试
- [x] 3.7 提供临时单块兼容开关并记录迁移说明
- [x] 3.8 映射 ACP token 用量并为 OpenAI/Anthropic 响应提供统一非零估算兜底

## 4. ACP 常驻运行时

- [x] 4.1 探测 Kiro CLI 2.14 ACP 的初始化、模型选择、会话、流式、取消和工具能力并形成兼容矩阵
- [x] 4.2 实现 ACP JSON-RPC 客户端、请求关联、通知分发、stderr 隔离和能力协商
- [x] 4.3 实现最小/最大 worker 数、最少负载调度和有界等待队列
- [x] 4.4 实现 worker 心跳、退出与协议错误检测、替换和退避重启
- [x] 4.5 实现 FastAPI lifespan 启停与 systemd 优雅关闭，确保不遗留 ACP 子进程
- [x] 4.6 实现 ACP 失败自动回退 `CliTransport` 并记录脱敏降级原因
- [x] 4.7 增加连续热请求、并发饱和、worker 崩溃、协议损坏和服务重启测试

## 5. 会话复用

- [x] 5.1 定义 OpenCode、Claude Code、OpenAI 和 Anthropic 客户端会话 ID 的提取优先级与响应回传头
- [x] 5.2 实现由租户/API Key 与外部会话 ID 组成的隔离会话键
- [x] 5.3 实现会话记录、worker 亲和性、上游会话 ID、规范化历史和同会话异步锁
- [x] 5.4 实现空闲 TTL、LRU 容量限制和定期清理任务
- [x] 5.5 实现 worker 故障后的历史重放恢复与无法恢复时的显式重建标记
- [x] 5.6 增加同会话串行、跨会话并行、跨 API Key 隔离、过期清理和故障恢复测试
- [x] 5.7 用真实 OpenCode 与 Claude Code 多轮会话验证上下文复用和 credits/延迟改善
- [x] 5.8 实现客户端压缩检测、ACP 会话轮换、有界上下文元数据和超限单次重试
- [x] 5.9 修复忙碌亲和 worker 导致的跨会话串行，验证同会话串行与跨会话并行

## 6. 自适应传输与直接 Runtime

- [x] 6.1 确认直接 Kiro Runtime 的官方支持边界、服务条款和所需协议，记录启用前置条件
- [x] 6.2 实现分别解析 Identity Center 区域与 profile ARN Runtime 区域的凭证元数据模型
- [x] 6.3 增加 `ap-southeast-2` Identity Center 与 `us-east-1` Profile 的跨区域回归测试
- [ ] 6.4 在默认关闭的实验开关后实现 `RuntimeTransport`、事件解析和错误分类
- [ ] 6.5 实现短期令牌读取、日志脱敏、一次受控刷新和刷新失败处理
- [x] 6.6 实现 Runtime/ACP/CLI 优先级配置、超时、熔断、冷却探测和逐级降级
- [ ] 6.7 增加 Runtime 401/403、连续 5xx、网络 EOF、协议变化和双重降级测试
- [ ] 6.8 验证三种传输对外产生一致的 OpenAI/Anthropic JSON、SSE 和错误结构

## 7. 性能与可靠性验收

- [x] 7.1 建立可重复的冷启动、热请求、首字、总时长、并发吞吐、内存和子进程基准脚本
- [x] 7.2 验证热请求代理固定开销不超过 500 毫秒
- [x] 7.3 验证首个上游文本事件在 100 毫秒内转发给客户端
- [x] 7.4 执行并发、长响应、客户端断开、ACP 崩溃、凭证过期和服务重启稳定性测试
- [x] 7.5 使用 OpenCode、Claude Code、OpenAI SDK 和 Anthropic SDK 完成端到端兼容测试
- [x] 7.6 检查日志、进程环境和错误响应中不存在 API Key、访问令牌及刷新令牌泄露

## 8. 发布与回滚

- [x] 8.1 为模型缓存、增量流、ACP、会话复用和 Runtime 分别增加独立配置开关及安全默认值
- [x] 8.2 更新 `.env` 示例、README、systemd 服务说明、API 兼容边界和故障排查文档
- [x] 8.3 按缓存、流式、ACP、会话、Runtime 的顺序逐阶段启用并记录每阶段验收结果
- [x] 8.4 验证可通过配置依次回滚 Runtime、会话复用、ACP 和增量流，最终恢复到 `CliTransport`
- [x] 8.5 清理临时兼容开关前确认所有已知客户端均通过标准增量 SSE 测试
